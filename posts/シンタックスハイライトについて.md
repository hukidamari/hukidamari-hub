---
slug: syntax-highlight
title:
published: true
tags: []
description:
thumbnail:
createdAt: 2025-12-03T16:49:43+09:00
updatedAt: 2025-12-03T16:49:43+09:00
---
シンタックスハイライトについては、[Prism](https://prismjs.com/) (より正確には [markdown-it-prism - npm](https://www.npmjs.com/package/markdown-it-prism)) を用いて実装しています。
スタイルについては、[Prism](https://prismjs.com/) から選んだり、好きに設定したりしてください。


## JavaScript

```js
const greet = (name) => {
  return `Hello, ${name}!`;
};

console.log(greet("World"));
````

### クラス

```js
class User {
  constructor(name) {
    this.name = name;
  }

  sayHi() {
    console.log(`Hi! I'm ${this.name}.`);
  }
}

const u = new User("Alice");
u.sayHi();
```

### 非同期処理

```js
async function fetchData() {
  const res = await fetch("/api/data");
  const json = await res.json();
  console.log(json);
}

fetchData();
```

---

## TypeScript

```ts
type User = {
  id: number;
  name: string;
};

function createUser(user: User): User {
  return { ...user, id: Date.now() };
}

const newUser = createUser({ id: 0, name: "Alice" });
console.log(newUser);
```

---

## Python

```python
def add(a, b):
    return a + b

print(add(3, 5))
```

```python
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        print(f"{self.name} makes a sound")

dog = Animal("Dog")
dog.speak()
```

```python
squares = [i * i for i in range(10)]
print(squares)
```

---

## Shell / Bash

```sh
#!/bin/bash

for i in 1 2 3; do
  echo "Count: $i"
done
```

```sh
if [ -f "./config.json" ]; then
  echo "Config found."
else
  echo "Config missing."
fi
```

---

## JSON

```json
{
  "name": "sample-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build"
  }
}
```

---

## PHP

```php
<?php
function greet($name) {
  return "Hello, $name!";
}

echo greet("World");
```

---

## Rust

```rust
fn main() {
    let nums = vec![1, 2, 3];
    for n in nums {
        println!("Number: {}", n);
    }
}
```

---

## Go

```go
package main

import "fmt"

func main() {
    for i := 1; i <= 3; i++ {
        fmt.Println("Count:", i)
    }
}
```

---

## C++

```cpp
#include <iostream>
using namespace std;

int main() {
    for (int i = 0; i < 5; i++) {
        cout << "i = " << i << endl;
    }
}
```

---

## CSS

```css
.container {
  display: flex;
  gap: 1rem;
}

.card {
  padding: 1rem;
  border-radius: 8px;
  background: #222;
  color: white;
}
```

---

## HTML

```html
<div class="card">
  <h1>Hello World</h1>
  <p>This is a sample.</p>
</div>
```

